# 应用配置
APP_NAME=AutoML Web Platform
DEBUG=True
API_PREFIX=/api
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# 数据库配置
DATABASE_URL=sqlite:///./storage/automl.db

# 文件存储配置
UPLOAD_DIR=./storage/uploads
MODEL_DIR=./storage/models
RESULT_DIR=./storage/results

# 训练配置
DEFAULT_TIME_LIMIT=3600
MAX_TIME_LIMIT=7200

# Server
HOST=0.0.0.0
PORT=8000
DEBUG=True

# Security
CORS_ORIGINS=["http://localhost:3000"]

# GPU配置 (自动检测，无需手动设置)
# 系统会自动检测是否有可用的GPU并相应调整 